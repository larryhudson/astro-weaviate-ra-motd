---
import { getMixById } from "@src/lib/weaviate/query";
import PlayerDetails from "@src/components/PlayerDetails.astro";

const weaviateObjectId = Astro.params.id;

// find mix with property mixId
const mix = await getMixById({
    weaviateObjectId,
});

export const partial = true;
---

<PlayerDetails {mix} />
